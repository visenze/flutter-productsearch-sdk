name: visenze_productsearch_sdk_example
description: Demo project for visenze_productsearch_sdk.
publish_to: 'none'

environment:
  sdk: '>=2.19.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  visenze_productsearch_sdk:
    path: ../
  file_picker: ^5.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
